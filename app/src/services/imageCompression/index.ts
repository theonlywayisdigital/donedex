/**
 * Image Compression Service
 * Platform-specific exports for native and web
 */

export { compressImage, compressImageToBase64 } from './imageCompression';
export type { CompressionOptions, CompressedImage } from './types';
